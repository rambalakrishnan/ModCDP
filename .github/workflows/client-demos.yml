name: Client Demos

on:
  pull_request:
  push:

jobs:
  client-test:
    name: ${{ matrix.client }} tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        client: [js, python, go]
    env:
      CHROME_PATH: /usr/bin/chromium
      CI: "true"
      GOFLAGS: -buildvcs=false
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - uses: actions/setup-python@v5
        if: matrix.client == 'python'
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v5
        if: matrix.client == 'python'
      - uses: actions/setup-go@v5
        if: matrix.client == 'go'
        with:
          go-version-file: go/go.mod
          cache-dependency-path: go/go.sum
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build
      - name: Run ${{ matrix.client }} tests
        run: |
          case "${{ matrix.client }}" in
            js)
              pnpm exec vitest run --fileParallelism=false
              ;;
            python)
              cd python
              uv run python -m unittest discover -s tests
              ;;
            go)
              cd go
              go test -count=1 ./...
              ;;
            *)
              echo "unknown client: ${{ matrix.client }}" >&2
              exit 1
              ;;
          esac

  client-demo:
    name: ${{ matrix.client }} ${{ matrix.mode }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        client: [js, python, go]
        mode: [loopback, debugger, direct]
    env:
      CHROME_PATH: /usr/bin/chromium
      CI: "true"
      GOFLAGS: -buildvcs=false
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - uses: actions/setup-python@v5
        if: matrix.client == 'python'
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v5
        if: matrix.client == 'python'
      - uses: actions/setup-go@v5
        if: matrix.client == 'go'
        with:
          go-version-file: go/go.mod
          cache-dependency-path: go/go.sum
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build
      - name: Run ${{ matrix.client }} demo (${{ matrix.mode }})
        run: |
          case "${{ matrix.client }}" in
            js)
              node dist/js/examples/demo.js --${{ matrix.mode }}
              ;;
            python)
              cd python
              uv run python examples/demo.py --${{ matrix.mode }}
              ;;
            go)
              cd go
              go run ./examples/demo --${{ matrix.mode }}
              ;;
            *)
              echo "unknown client: ${{ matrix.client }}" >&2
              exit 1
              ;;
          esac

  proxy-example:
    name: ${{ matrix.name }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: playwright proxy
            command: node dist/js/examples/playwright.js
          - name: puppeteer proxy
            command: node dist/js/examples/puppeteer.js
    env:
      CHROME_PATH: /usr/bin/chromium
      CI: "true"
      GOFLAGS: -buildvcs=false
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build
      - run: ${{ matrix.command }}
